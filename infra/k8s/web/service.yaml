apiVersion: v1
kind: Service
metadata:
  name: eduai-web
  namespace: eduai
  labels:
    app.kubernetes.io/name: eduai-web
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: eduai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: eduai-web
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
